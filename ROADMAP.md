# 다음 작업 목록

앞으로 진행하기로 한 기능들과 순서입니다. 항목별로 완료되면 체크하고, 필요하면 순서나 내용을 자유롭게 고쳐도 됩니다.

## 진행 순서

1. [x] 다크모드
2. [x] 3MF 파일 다운로드
3. [x] 로컬 폰트 업로드
4. [x] 모바일 반응형

**순서를 이렇게 정한 이유**: 다크모드/3MF/폰트 업로드는 각각 화면에 새 버튼(테마 토글, 3MF 다운로드 버튼, 폰트 업로드 옵션)을 추가하는 작업입니다. 모바일 대응은 결국 "이 모든 요소가 좁은 화면에서도 잘 배치되게 만드는" 작업이라, UI 구성이 최종적으로 확정된 뒤 마지막에 한 번에 처리하는 게 버튼 추가할 때마다 반응형을 다시 손보는 것보다 효율적입니다.

---

## 1. 다크모드

- **적용 범위**: `assets/css/core.css` + `assets/js/core.js`(편집화면 8개 공용) + `index.html`(메인화면, 별도 스타일) + 3D 뷰어 배경색(현재 `core.js`의 `initViewer()`에서 `scene.background`가 밝은 회색으로 고정돼 있음 — 이것도 테마에 맞게 바꿔야 패널과 뷰어가 안 어색함).
- **연동**: 메인화면과 편집화면의 다크모드 선택은 자동으로 연동됨(같은 출처라 localStorage 값을 공유하면 됨, 별도 동기화 코드 불필요).
- **버튼 위치**: 편집화면에서는 뷰큐브(시점 조작 버튼, `#viewcube-container`) 바로 아래에 배치. 메인화면 위치는 미정(우측 상단 등 자유롭게 결정).
- **권장 방식**: 처음엔 시스템 설정(OS 다크모드 여부)을 따르되, 사용자가 토글을 클릭하면 그 선택을 localStorage에 기억해두는 방식(깃허브 등에서 흔한 패턴). `<html>`에 `data-theme="dark"/"light"` 속성을 주는 식으로 적용하고, 첫 렌더링 전에 테마를 미리 적용하는 짧은 인라인 스크립트를 `<head>`에 둬서 깜빡임(flash) 방지.
- 갤러리 8개 각각의 개별 `<style>` 블록(격자 토글 색상, 색상 선택 줄 등)에도 하드코딩된 밝은 색이 있어서, 공용 CSS뿐 아니라 이쪽도 한 번씩 손봐야 완전히 일관됨.

## 2. 3MF 파일 다운로드

- three.js(이 프로젝트가 고정해 쓰는 r146 버전)에는 3MF 내보내기 기능이 기본으로 없음(불러오기용 3MFLoader만 있음). 3MF는 ZIP 안에 XML을 담는 포맷이라, 압축 없는 ZIP(STORED 방식)로 직접 만들면 별도 라이브러리 없이도 구현 가능 — 이 프로젝트가 이미 쓰고 있는 "빌드 과정 없이 직접 만든 브릿지" 방식(`manifold-core.js`, `font-core.js`)과 같은 결.
- **단순 포맷 교체가 아니라 실질적 이득이 있음**: STL은 색 정보를 담을 수 없는 포맷인데, 이미 여러 갤러리(멀티컬러 이름표, 클리커 뚜껑 등)가 부품별로 다른 색을 가진 모델을 만들고 있음(`ManifoldCore.toThreeMesh(fused, idToColorMap, ...)`). 3MF로 내보내면 이 색 정보가 그대로 보존됨.
- STL 다운로드 버튼은 그대로 두고 3MF를 추가 버튼으로 넣는 방식 추천(STL이 아직 더 널리 호환되고, 3MF는 최신 슬라이서에서 특히 색상 면에서 잘 지원됨).

## 3. 로컬 폰트 업로드

- 사용자가 자기 컴퓨터에 있는 폰트 파일(.ttf/.otf)을 직접 선택해서 쓸 수 있게 하는 기능.
- **핵심 로직은 단순함**: 파일 선택 → `File.arrayBuffer()`로 읽기 → `opentype.parse(arrayBuffer)`로 분석. 이미 쓰고 있는 `opentype.load(url, callback)` 방식과 결과물 형태가 같아서, 글자 윤곽선을 뽑는 나머지 코드(`buildCenteredContours` 등)는 손댈 필요 없음. 전부 브라우저 안에서만 처리되고 서버에 올라가지 않으므로, 배포된 폰트들과 달리 라이선스 문제도 없음.
- 검토했지만 채택하지 않은 방법: 브라우저의 "Local Font Access API"(`navigator.fonts.query()`) — 파일 선택 없이 OS에 설치된 폰트 목록을 바로 보여줄 수 있지만, 크롬/엣지 계열만 지원하고(사파리·파이어폭스 불가) 권한 요청이 다소 부담스러워서 제외.
- **실제 작업량은 반복 배선이 대부분**: 폰트 선택 드롭다운이 있는 갤러리마다(다국어 키링, 이름표, 클리커 뚜껑 등 8개 중 3~4개) 업로드 옵션을 각각 추가해야 함. 텍스트 1/2/3처럼 줄마다 폰트를 따로 고르는 이름표 갤러리는 어느 줄에 어떤 업로드 폰트를 적용할지 추적하는 처리가 조금 더 필요함. `font-core.js`의 글자 모양 캐시 키가 파일명(`fontFile`) 기준이라, 업로드된 폰트는 별도의 캐시 키를 만들어줘야 함. 깨진 파일에 대한 에러 처리, `.ttf/.otf` 위주로 파일 선택창 제한(이 버전 opentype.js의 woff/woff2 지원은 불확실해서 사용 전 확인 필요)도 필요함.

## 4. 모바일 반응형

- 가장 크고 손이 많이 가는 항목. 지금 편집화면 레이아웃(`#controls` 고정폭 사이드바 + `#viewer`, `core.css`에서 `<body>`에 `display:flex`)은 데스크톱 전제라 모바일 대응 브레이크포인트가 아직 없음. 폰 화면 폭에서는 사이드바가 화면 대부분을 차지해버림.
- 필요한 작업: 좁은 화면에서 사이드바를 접이식/하단시트 형태로 전환, 드래그 리사이저(`#resizer`, 마우스 전용 상호작용)를 터치 환경에서 숨기기, OrbitControls의 터치 제스처(회전/확대)가 실제로 잘 동작하는지 확인, 작은 상호작용 요소(예: 클리커 몸체/뚜껑 갤러리의 5×5 격자 버튼, 셀당 약 38px)의 터치 크기 점검, 뷰큐브·다크모드 토글·다운로드 버튼들이 좁은 화면에서 겹치지 않는지 재확인.
- `index.html`의 카드 그리드는 이미 부분적인 모바일 대응이 되어 있음(`@media (max-width: 720px)`에서 `auto-fit, minmax(220px, 1fr)`로 전환) — 편집화면 쪽은 아직 전무함.
- **성능 위험 요소**: 이번 세션에서 멀티컬러 이름표 갤러리가 최적화 후에도 미리보기 정점 수가 100만 개를 넘는 것을 확인함 — 데스크톱 GPU에서는 괜찮지만 저사양 모바일 기기에서는 버벅일 가능성이 있음. 이 단계에서 레이아웃/터치뿐 아니라 갤러리 전반의 정점 수 점검도 함께 하면 좋음.
